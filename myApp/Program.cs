using System;
using System.Data.SqlClient;

namespace myApp
{
    class Program
    {
        /**
        * Connecting to SQL database
        * string
        *    conString =
        *        @"Data Source=localhost; Initial Catalog=expiry_registration;User ID=sa;Password=20304wW.";
        * SqlConnection conn = new SqlConnection(conString);
        */
        static void Main(string[] args)
        {
            Console
                .WriteLine("<========== Getting a database connection ============>");
        }

        /**
        * Connecting to mysql
        **/
    }
}
